/**
 * src/index.js — entry (this repo uses index.js instead of Deskhub’s main.js)
 *
 * Day 28 — DAILY_TASKS.md (Deskhub main.js section):
 *   [ ] Import bootstrap from ./modules/auth.js (e.g. initApp)
 *   [ ] Call it once when the module loads
 *
 * Deskhub suggests dispatching via document.body.dataset.page; this project uses one index.html with two
 * <main> sections instead — your modules/auth.js should own that wiring.
 *
 * Pitfalls: HTML must load this with <script type="module" src="/src/index.js"></script>
 */

// import { initApp } from "./modules/auth.js";

// initApp();
