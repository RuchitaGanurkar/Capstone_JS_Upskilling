/**
 * api/auth.js — fake auth against json-server /users
 *
 * Day 28 — DAILY_TASKS.md:
 *   [ ] login(email, password) — verify user (e.g. GET /users?email=…&password=…); store token + user in storage;
 *        do NOT persist password; return a safe user shape
 *   [ ] logout() — remove token + user from storage
 *   [ ] getCurrentUser() — read user from storage or null
 *   [ ] isAuthenticated() — true if token exists (or your chosen rule)
 *
 * Done when: priya@deskhub.in / demo123 works; wrong password throws or signals failure for the UI.
 *
 * Pitfalls: json-server returns an array from GET /users?…; expect 0 or 1 match
 */

// import * as storage from "../utility/storage.js";
// import { get } from "./client.js";

// const TOKEN_KEY = "token";
// const USER_KEY = "user";

// export async function login(email, password) {
//   // TODO Day 28
// }

// export function logout() {
//   // TODO Day 28
// }

// export function getCurrentUser() {
//   // TODO Day 28
// }

// export function isAuthenticated() {
//   // TODO Day 28
// }
